# 指标 1.2 PPT 文案

指标 1.2：异构计算架构适配国产 CPU、GPU、NPU 等计算资源

## 测试一：寒武纪架构上推理任务运行测试

完成寒武纪架构上推理任务运行验证，初步完成面向国产 NPU 异构计算资源的大模型推理任务适配能力验证。  
测试硬件：寒武纪异构计算平台 / 单机双卡 MLU580 服务器。  
测试任务：大模型推理任务（Llama3.1-8B）。  
测试基线：以任务是否可正常启动、执行、结束并输出正确推理结果为基线。  
测试方案：  
1. 在搭载英特尔 CPU + 寒武纪 NPU 的服务器上配置 Docker、Torch-MLU、模型目录和推理运行环境。  
2. 选取 `Llama3.1-8B` 模型，分别在单卡和单机双卡两种规模下启动推理任务，检查硬件资源可见性、任务启动状态和执行日志。  
3. 记录任务是否正常结束，并检查推理输出是否生成、是否存在硬件识别错误、显存溢出或核心转储等异常。  
预期结果：功能正常，满足异构计算架构适配国产 NPU 设备的指标要求。  

该测试项完成指标 1.2 中寒武纪架构推理适配验证的相关内容。  
完成寒武纪架构上推理任务运行测试，验证了 `Llama3.1-8B` 在单卡与单机双卡两种规模下均可正常启动、执行并输出结果。  
测试硬件：寒武纪异构计算平台 / 单机双卡 MLU580 服务器。  

当前实际结果：  
- 单卡推理成功，结果见 [c1b1b382/summary.json](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/inference/c1b1b382/summary.json)  
- 双卡推理成功，结果见 [180d4000/summary.json](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/inference/180d4000/summary.json)  
- 两组运行均成功输出文本：`alpha alpha alpha alpha`  
- 运行过程中未出现硬件识别失败、显存溢出或核心转储错误  

## 测试二：寒武纪架构上训练任务运行测试

完成寒武纪架构上训练任务运行验证，初步完成面向国产 NPU 异构计算资源的大模型训练任务适配能力验证。  
测试硬件：寒武纪异构计算平台 / 单机双卡 MLU580 服务器。  
测试任务：大模型训练任务（Llama3.1-8B）。  
测试基线：以任务是否可正常启动、执行、结束，并成功输出损失曲线与 checkpoint 验证产物为基线。  
测试方案：  
1. 在搭载英特尔 CPU + 寒武纪 NPU 的服务器上配置 Docker、Torch-MLU、模型目录和训练运行环境。  
2. 选取 `Llama3.1-8B` 模型，分别在单卡和单机双卡两种规模下启动训练任务，检查资源调度、前反向传播、参数更新和运行日志。  
3. 记录训练是否正常结束，检查是否生成损失函数变化记录、损失曲线文件以及 checkpoint 验证产物，并确认无通信错误或训练崩溃。  
预期结果：功能正常，满足异构计算架构适配国产 NPU 设备的指标要求。  

该测试项完成指标 1.2 中寒武纪架构训练适配验证的相关内容。  
完成寒武纪架构上训练任务运行测试，验证了 `Llama3.1-8B` 在单卡与单机双卡两种规模下均可完成训练迭代、参数更新和结果保存。  
测试硬件：寒武纪异构计算平台 / 单机双卡 MLU580 服务器。  

当前实际结果：  
- 单卡训练成功，结果见 [8b0e444b/summary.json](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/training_local/8b0e444b/summary.json)  
- 双卡训练成功，结果见 [166410aa/summary.json](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/training_local/166410aa/summary.json)  
- 单卡损失下降：`7.6432 -> 6.0636 -> 4.6450`  
- 双卡损失下降：`7.6432 -> 6.0636 -> 4.6450`  
- 已生成损失曲线与 checkpoint 验证产物：  
  - [8b0e444b/loss_curve.svg](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/training_local/8b0e444b/loss_curve.svg)  
  - [166410aa/loss_curve.svg](/home/o_mabin/hanwuji-clj-proj/projects/indicators/1.2-runtime-validation/reports/runs/training_local/166410aa/loss_curve.svg)  
- 运行过程中未出现训练崩溃或通信中断，满足“任务成功完成且模型可保存”的判定要求。  
